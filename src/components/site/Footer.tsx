import { ArrowUpRight, Mail, Phone } from "lucide-react";
import { company } from "@/config/company";
import { siteContent } from "@/config/content";
import { navigation } from "@/config/navigation";

export function Footer() {
  return <footer className="bg-[#0b1120] text-white"><div className="mx-auto grid max-w-7xl gap-10 px-5 py-14 sm:px-6 lg:grid-cols-[1.2fr_.7fr_.7fr]"><div><a href="#home" className="text-xl font-semibold tracking-[-.03em]">{company.name}</a><p className="mt-3 max-w-sm text-sm leading-6 text-slate-400">{company.tagline}</p><p className="mt-6 max-w-md text-xs leading-5 text-slate-500">{siteContent.footer.disclaimer}</p></div><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-[#D4AF37]">Explore</p><nav className="mt-4 grid gap-3">{navigation.map((link) => <a key={link.label} href={link.href} className="text-sm text-slate-300 transition hover:text-white">{link.label}</a>)}</nav></div><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-[#D4AF37]">Connect</p><div className="mt-4 grid gap-3 text-sm text-slate-300"><a href={`tel:${company.phone}`} className="flex items-center gap-2 transition hover:text-white"><Phone size={15} />{company.phone}</a><a href={`mailto:${company.email}`} className="flex items-center gap-2 transition hover:text-white"><Mail size={15} />{company.email}</a><a href="#contact" className="mt-2 flex items-center gap-2 font-medium text-[#D4AF37]">Book a consultation <ArrowUpRight size={15} /></a></div></div></div><div className="border-t border-white/10"><div className="mx-auto flex max-w-7xl flex-wrap justify-between gap-2 px-5 py-5 text-xs text-slate-500 sm:px-6"><span>© {new Date().getFullYear()} {company.name}. All rights reserved.</span><span>MahaRERA: {company.mahaReraNumber}</span></div></div></footer>;
}
